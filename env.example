# RAG Chatbot - Environment Variables Template

# Copy this file to .env and fill in your API keys

# LLM Provider Selection: "openai" or "gemini"
LLM_PROVIDER=gemini

# Embedding Provider Selection: "local" (free, recommended), "openai", or "gemini"
# Note: Gemini free tier has quota limits for embeddings. Use "local" for free tier.
EMBEDDING_PROVIDER=local

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini Configuration (if using Gemini)
GOOGLE_API_KEY=your_google_api_key_here

# Model Configuration
MODEL_NAME=gemini-2.5-flash
# For OpenAI: gpt-3.5-turbo, gpt-4, etc.
# For Gemini: gemini-pro, gemini-2.5-flash, etc.

# Local Embedding Model (only used if EMBEDDING_PROVIDER=local)
# Options: all-MiniLM-L6-v2 (default, fast), all-mpnet-base-v2 (better quality)
LOCAL_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Vector Store Configuration
VECTOR_STORE_PATH=./vector_store
DOCUMENTS_PATH=./documents

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Retrieval Configuration
TOP_K_RESULTS=7

